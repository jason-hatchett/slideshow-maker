<section class="banner-all">
      <%= image_tag("cloud7.png", :size => "150x150")%>
      <a href="/" class="button radius" id="searchbutton"> SEARCH VIDEOS </a>
      <a href="/" class="button radius" id="createbutton"> CREATE VIDEO </a>
      <br>
      <br>
 </section>

<%= form_tag("/search", name: "hashform", :onclick => "return hashtagSubmit();") do %>
  <%= label_tag(:tag, "Enter Hashtag:") %>
  <%= text_field_tag(:tag) %>
  <%= submit_tag("Search") %>
<% end %>



<!--
  <form onsubmit="hashtagSubmit()" method="post" id="my_form">
      <label for="tag">EVENT HASHTAG:</label>
      <input type="text" id="tag" name="tag" placeholder="your event hashtag">
      <button class="btn">Submit</button>
    </form>
<br> -->


<% @data["data"].take(3).each do |x|%>

   <img src="<%= x["images"]["low_resolution"]["url"] %>" alt="">

<% end %>






